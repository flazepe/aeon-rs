fn main() {
    /*
    tonic_build::configure()
        .build_client(true)
        .build_server(false)
        .out_dir("src/structs/api/google/protos")
        .compile_protos(&["google/assistant/embedded/v1alpha2/embedded_assistant.proto"], &["proto"])
        .unwrap();
    */
}
